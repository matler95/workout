// DELETED — removed from Dashboard.
// Injury risk detection (detectInjuryRisk in smartAlgorithms.ts) is preserved
// but no longer surfaced as a card. High-severity signals are folded into the
// SmartInsights deload/fatigue warnings.
export function InjuryRiskAlerts() { return null; }
