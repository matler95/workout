// DELETED — replaced by inline rep feedback in ActiveWorkout.tsx.
// The two-card guidance block during workout has been removed.
// Rep range hint is now a single line of text below the inputs.
export function SmartWorkoutGuidance() { return null; }
