// DELETED — ProgressInsights cards (bodyweight projection, work capacity,
// muscle balance) removed from the top of the Progress page.
// These were shown before users had enough data to make them meaningful.
// The underlying algorithms (predictBodyweightTrend, calculateWorkCapacity,
// calculateMuscleBalance) are preserved in smartAlgorithms.ts for future use.
export function ProgressInsights() { return null; }
